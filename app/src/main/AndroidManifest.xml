<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.makeinfo.andenginetemplate">

    <uses-sdk android:minSdkVersion="17" />

    <android:uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <android:uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <android:uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.MenuActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.ModeSelectActivity" />
        <activity android:name=".Activities.LevelSelectors.ClassicLSActivity"/>
        <activity android:name=".Activities.LevelSelectors.MirrorLSActivity"/>
        <activity android:name=".Activities.LevelSelectors.TimeraceLSActivity"/>
        <activity android:name=".Activities.LevelSelectors.SnakeLSActivity"/>
        <activity android:name=".Activities.GameActivity"/>
    </application>

</manifest>